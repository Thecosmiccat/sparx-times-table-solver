# Sparx Solver Pro — runtime dependencies
# Python 3.12 recommended on macOS.
# Tkinter is not installed via pip: brew install python-tk@3.12

customtkinter>=5.2.0,<6
easyocr>=1.7.0,<2
pyautogui>=0.9.54
Pillow>=10.0.0
numpy>=1.24.0,<3
sympy>=1.12

# easyocr pulls in: torch, torchvision, opencv-python-headless, scipy,
# scikit-image, and others. pyautogui pulls in pyobjc-* on macOS.
